# Smart Document Scanner — Python dependencies
# Install with: pip install -r requirements.txt

# Computer vision — used for camera capture, edge detection, perspective warp
opencv-python>=4.8.0

# Image processing — used for PDF generation and final touch-ups
Pillow>=10.0.0

# Numerical arrays — OpenCV uses it internally, but we also use it for geometry math
numpy>=1.24.0

# Web UI — serves the gallery + PDF download endpoint on localhost
Flask>=3.0.0

# QR code generator — embeds a download link onto the generated PDF
qrcode>=7.4.0

# NOTE: ``ultralytics`` (YOLOv8n) was removed.  The bundled weights failed to
# load on the Pi and every frame fell back to the OpenCV contour detector
# anyway, so the dependency (which drags in torch — a huge install on a
# Raspberry Pi) bought nothing.  Document localisation is pure OpenCV now.

# PDF reader — used only by the smoke tests to verify generated PDFs
pypdf>=4.0.0 ; python_version < "3.13"

# Perceptual hashing — used by ``page_change_detector.py`` to confirm a new
# page was placed under the camera (Hamming distance over 64-bit phash).
imagehash>=4.3.1

# ---------------------------------------------------------------------------
# Optional — only needed when migrating to Raspberry Pi 5 + Camera Module 3
# ---------------------------------------------------------------------------
# picamera2>=0.3.12

# ---------------------------------------------------------------------------
# Optional — only needed on the Raspberry Pi 5 + MAX98357A I2S amp deployment
# to play the long-form ``captured.mp3`` / ``deleted.mp3`` cues.  Without
# these the ``mp3_player`` module silently no-ops, so the dev box keeps
# working fine without them installed.
# ---------------------------------------------------------------------------
# pydub>=0.25.1
# pyalsaaudio>=0.11.0
# ffmpeg (system package: ``sudo apt install -y ffmpeg``)